# WebDriver Configuration
webdriver.browser=chrome
webdriver.remote=false
webdriver.remote.url=https://hub-cloud.browserstack.com/wd/hub
webdriver.implicit.wait=10
webdriver.page.load.timeout=30

# Percy Configuration
percy.project.name=Bajaj-Visual-Tests
percy.screenshot.fullPage=true

# Test Configuration
test.retry.count=3
test.parallel.threads=1